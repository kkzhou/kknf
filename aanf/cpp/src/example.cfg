server: {
    listen_socket: (
        {
            name = data_channel
            ip = 127.0.0.1
            port = 10001
            ype = tcp
            format = bin
        },
        {
            name = admin_channel
            ip = 127.0.0.1
            port = 10002
            socket_type = tcp
            format = line
        }

    )

    my_connect_ip = 127.0.0.1   #我用于主动连接的IP，0.0.0.0表示任意
    my_connect_port = 0          #我用于主动连接的端口，0表示任意

    worker_num = 4  #工作线程的个数
    send_queue_num = 1 #发送队列个数，一般用于优先级队列

    log_server_ip = 127.0.0.1   #日志服务器的IP
    log_server_port = 10003     #日志服务器的Port
    log_server_type = tcp
    log_server_data_format = bin
    log_id = 100

    report_server_ip = 127.0.0.1    #上报服务器的IP
    report_server_port = 10004      #上报服务器的Port
    report_interval = 600           #上报的间隔，单位秒
    report_server_type = tcp
    report_server_data_format = bin
    report_id = 100

    config_server_ip = 127.0.0.1    #配置服务器的IP
    config_server_port = 10005      #配置服务器的Port
    config_check_interval = 60      #配置文件的检查时间间隔


}
